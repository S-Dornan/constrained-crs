# Infrastructure as Code

Infrastrucutre as code scripts to run the system go here

Current template is Arc-enabled Kubernetes from the CRS-Kubernetes repo (which is Docker in a frock, to be polite about it)